import { Section } from "@/components/common/Section"
import { Outlet } from "react-router-dom"

export const AccountMobileLayout = () => {
    return (
        <Section>
            {/* haloooo kawanku */}
            <Outlet />
        </Section>
    )
}